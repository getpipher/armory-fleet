// test/smoke.test.mts — placeholder so the suite is green from scaffolding.
import { test } from "node:test";
import { strictEqual } from "node:assert";

test("scaffold ok", () => {
  strictEqual(1 + 1, 2);
});